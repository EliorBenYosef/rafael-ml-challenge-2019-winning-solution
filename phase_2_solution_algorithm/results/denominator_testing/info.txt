if config == 0:
    denominator *= (self.t_to_fire_angle + 1)  # handles the 't_to_fire_angle == 0' problem
# further penalize high t_to_fire_angle missiles:
elif config == 1:
    denominator *= (self.t_to_fire_angle + 1) if self.t_to_fire_angle > fire_action_range else 1  # i used this
elif config == 2:  # doesn't work
    denominator *= (self.t_to_fire_angle + 1) if self.t_to_fire_angle >= fire_action_range + fire_threshold else 1

0: denominator *= (self.t_to_fire_angle + 1)  # handles the 't_to_fire_angle == 0' problem
1: denominator *= (self.t_to_fire_angle + 1) if self.t_to_fire_angle > fire_action_range else 1  # i used this
2: denominator *= (self.t_to_fire_angle + 1) if self.t_to_fire_angle >= fire_action_range + fire_threshold else 1

chosen: 1
np seed: 28